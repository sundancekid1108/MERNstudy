export default (theme) => ({
    root: {
        borderRadius: '4px',
        maxWidth: '100%',
        border: 0,
        boxShadow: '0 10px 40px 0 rgba(16, 36, 94, 0.2)'
    },
    squared: {
        borderRadius: 0
    },
    outlined: {
        border: 0
    }
});